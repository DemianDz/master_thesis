  name: Cypress tests
  on: push
  jobs:
    cypress-tests:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v3.1.0
         - name: Smoke tests
           run: npm run cy:run:cypress
